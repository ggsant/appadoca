import 'package:flutter/material.dart';

class ThemeColors {
  // Font Colors
  static const Color primaryTextColor = Colors.black;
  static const Color secondaryTextColor = Colors.white;
  static const Color primaryColor = Color.fromRGBO(255, 255, 255, 0.5);
  static const Color secondaryColor = Color.fromRGBO(243, 236, 230, 1);
  static const Color terciaryColor = Color.fromRGBO(32, 32, 32, 1);
  static const Color backgroundColor = Color(0xFF202020);
  static Color kPrimaryColor = Colors.pink[800];
}
